defmodule Trunk.ProcessorTest do
  use ExUnit.Case, async: true

  describe "store/1" do
    test "transforms each version"
    test "correctly saves each version"
  end

  describe "generate_url/1" do

  end
end
